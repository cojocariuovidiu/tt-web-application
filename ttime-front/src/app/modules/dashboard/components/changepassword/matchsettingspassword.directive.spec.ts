import { MatchsettingspasswordDirective } from './matchsettingspassword.directive';

describe('MatchsettingspasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchsettingspasswordDirective();
    expect(directive).toBeTruthy();
  });
});
